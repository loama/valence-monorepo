# Valence App

Next.js, Capacitor, and shadcn workspace for the authenticated Valence product.

The web deployment is routed at `/app`. For a local mobile export, run:

```bash
bun run --cwd app build:mobile
bun run --cwd app cap:sync
```
